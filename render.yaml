services:
  - type: web
    name: ml-service
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn predict:app"
    runtime:
      python: 3.11.6
